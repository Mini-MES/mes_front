# Mini-MES 통합 테스트 가이드

> [솔루션 검수 문서](./docs/mes_solution_acceptance.md)의 AC-01~AC-09를 실제 프론트엔드·백엔드·OPC 환경에서 검증하는 상세 절차입니다.

## 1. 준비 사항

- `mes_server`, SQL Server 및 `mes_front` 실행
- 관리자·작업자 계정, 제품, BOM, 공정 순서, 원자재 재고, 불량/비가동 사유 기준정보 준비
- OPC 검수 시 Kepware Endpoint `opc.tcp://127.0.0.1:49320`와 CNC의 `Counter`, `Running`, `Temperature` 태그를 정상 상태로 준비
- 관리자와 작업자 화면을 별도 창에서 동시에 연다.

## 2. 전체 검증 흐름

```text
원자재·기준정보 확인 → WorkOrder 생성 및 LOT 발행 → 설비 선택 생산 시작
→ OPC Counter 양품 자동 수집 → 불량 등록 / HOLD / 보류 해제 → 공정 이동·재작업
→ 비가동 사유 등록 → 생산 마감 → LOT 추적·OEE 확인 → 완제품 출하
```

## 3. TC-01. 자재 재고와 작업지시 생성

1. 관리자 화면에서 원자재 재고·안전재고를 확인하거나 품목을 등록한다.
2. 제품과 계획 수량으로 작업지시를 생성하고 LOT 추적 화면을 확인한다.

통과 기준:

- 자재 수량과 부족 상태가 정확히 표시된다.
- WorkOrder와 대상 LOT이 생성되고 첫 공정이 배정된다.
- 제품, 목표 수량, 시작일 및 납기일이 정확하며 두 화면의 목록이 갱신된다.

예외 확인: 원자재 부족 상태에서는 생산 시작이 거부된다.

## 4. TC-02. 설비 선택 생산 시작

1. 작업자 화면에서 WorkOrder·LOT을 선택하고 사용할 설비를 선택해 생산을 시작한다.
2. Network에서 아래 요청을 확인한다.

```http
POST /api/Production/start/{orderId}

{ "lotId": "LOT-...", "equipmentID": "CNC01" }
```

통과 기준:

- WorkOrder는 생산 중, LOT은 WIP가 된다.
- 선택한 설비만 해당 LOT의 `CurrentLotId`와 연결된다.
- 설비 상태·온도·수량이 작업자 화면에 표시된다.

예외 확인: 다른 LOT 점유, 사용 불가 설비 또는 원자재 부족 시 시작이 거부된다.

## 5. TC-03. OPC Counter 양품 자동 수집

1. LOT이 연결되지 않은 설비의 Counter를 증가시킨다.
2. 생산 시작 뒤 선택 설비의 Counter를 1, 다시 5 증가시킨다.
3. LOT·생산 수량·설비·OEE를 확인한다.

통과 기준:

- LOT이 없는 설비의 Counter는 실적을 만들지 않는다.
- 연결 설비의 증가량만 현재 LOT·공정 양품으로 반영되고 `InputQty`, `GoodQty`와 일치한다.
- 프론트는 자동 수집 양품을 `/Production/performance/register`로 중복 전송하지 않는다.
- 설비 생산량과 OEE 관련 화면이 갱신된다.

예외 확인: Counter Reset/감소 시 음수 실적이 생성되지 않으며 다른 설비 실적으로 기록되지 않는다.

## 6. TC-04. 불량 등록, HOLD 및 재작업

1. 작업자가 불량 수량과 사유를 등록한다.
2. LOT 상태·알림을 확인한 뒤 관리자가 HOLD를 해제하고 생산을 재개한다.

통과 기준:

- 불량은 `GoodQty = 0`, `BadQty = 입력 수량`, `InputQty = 입력 수량` 및 사유 코드로 저장된다.
- LOT은 HOLD가 되고 추가 실적·공정 이동이 차단된다.
- 보류 해제 후 LOT이 다시 작업 가능하며 두 화면이 동기화된다.

예외 확인: 사유가 없거나 수량이 유효하지 않은 불량은 등록이 거부된다.

## 7. TC-05. 공정 이동과 재작업

1. 현재 공정의 생산 수량을 확인하고 다음 공정 이동을 실행한다.
2. 필요한 경우 다음 설비를 지정하고 LOT 추적 화면에서 이력을 확인한다.

통과 기준:

- LOT 현재 공정이 공정 순서에 맞게 변경된다.
- 기존 OPC 누적 양품과 이전 실적이 다시 등록되지 않는다.
- 0수량 이동으로 Performance가 중복 생성되지 않으며 재작업 이력이 표시된다.

## 8. TC-06. 비가동 기록과 사유 등록

1. 생산 설비를 비가동 상태로 만들고 열린 비가동 이력을 확인한다.
2. 작업자가 비가동 사유와 메모를 저장하고 관리자 화면의 설비 현황·OEE를 확인한다.

통과 기준:

- 설비, 시작·종료 시각, 비가동 시간, 사유와 메모가 이력에 기록된다.
- 비가동 이력과 일일 설비 생산/OEE 화면이 갱신된다.

## 9. TC-07. 생산 마감

1. 최종 공정 양품 수량을 확인하고 작업지시 마감을 실행한다.
2. WorkOrder, LOT, 설비 연결 및 완제품 재고를 확인한다.

통과 기준:

- WorkOrder와 LOT은 완료 상태가 된다.
- 설비의 `CurrentLotId`가 해제되고 유휴 상태로 표시된다.
- 최종 양품 수량이 완제품 재고에 반영되며, 완료 후 추가 OPC 실적이 등록되지 않는다.

## 10. TC-08. LOT 추적과 일일 OEE

1. 관리자 화면에서 LOT의 현재 공정·상태·이력을 확인한다.
2. OEE 분석에서 설비별 생산량, 가동·비가동 시간 및 일일 OEE를 확인한다.

통과 기준:

- LOT 추적 데이터가 WorkOrder 및 공정 이력과 일치한다.
- 설비별 생산·비가동 데이터가 OEE 분석에 반영된다.

## 11. TC-09. 완제품 출하

1. 완료된 작업지시의 제품을 선택한다.
2. 출하 수량과 납품처를 입력하고 출하한 뒤 재고·출하 이력을 확인한다.

통과 기준:

- 완제품 재고가 출하 수량만큼 차감된다.
- 출하 수량, 납품처와 작업지시 정보가 출하 이력에 기록된다.

예외 확인: 보유 재고를 초과한 출하는 거부된다.

## 12. 실시간 동기화 공통 기준

작업지시 생성, 생산 시작, Counter·온도 변경, 불량/HOLD/해제, 공정 이동, 비가동 사유 등록, 마감과 출하마다 다음을 확인한다.

- SignalR 이벤트 또는 Mutation 성공 후 관련 React Query 캐시가 갱신되어 최신 서버 상태가 표시된다.
- 동일 이벤트로 수량·알림·이력이 중복 표시되지 않는다.
- WorkOrder, LOT, 설비, 재고 및 OEE가 REST 응답과 화면에서 일치한다.

## 13. 최종 합격 기준

- TC-01~TC-09가 모두 통과한다.
- 작업지시 → LOT → 생산 → 공정 이동 → 마감 → 출하 흐름이 끊기지 않는다.
- 자동 양품 수집과 수동 불량 등록이 중복 처리되지 않는다.
- HOLD·재작업·비가동이 LOT·설비 이력과 OEE에 일관되게 반영된다.
- Critical 또는 High 수준의 실행 오류 없이 전체 시나리오를 완료할 수 있다.
